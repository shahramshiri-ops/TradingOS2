# PRV1-01 Stage 3 Scripts

Created at UTC: 2026-05-04T10:43:38Z

Use `run_refresh.bat` on Windows or `python scripts/run_refresh.py --package-root . --print-summary` from the `personal_runtime_v1` root.

These scripts are dry-run/cache-first and display-only. They do not perform live fetch, read API keys, connect to brokers, create orders, generate signals, compute PnL, run optimizers, issue validation verdicts, make adaptation decisions, or claim production readiness.
